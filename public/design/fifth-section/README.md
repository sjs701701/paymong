# Fifth Section Mask

Replace this file to change the section 5 -> 6 reveal shape:

- `C:\Users\consult_04\Paymong\public\design\fifth-section\mask-shape.svg`

Notes:

- Keep one main `<path>` in the SVG when possible.
- Include a `viewBox` so the reveal can stay centered on the cursor.
- The component reads the first `<path>` and uses that path for the expanding mask.
